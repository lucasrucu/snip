// Shared links + copy for the public landing page. Keeping CTA targets in one
// place so the nav, hero, and footer stay in sync.

// Snip has real Google auth — the single CTA sends visitors to /login.
export const SIGN_IN_HREF = "/login";

export const GITHUB_HREF = "https://github.com/lucasrucu/snip";

export const APP_NAME = "Snip";

// The production short-link domain, shown in hero/footer copy.
export const SHORT_DOMAIN = "links.qori.land";
